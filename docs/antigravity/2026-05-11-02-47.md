# Slipwise — Project Context Snapshot
**Generated:** 2026-05-11 at 02:47 IST  
**Repository:** `payslip-generator` — `/Users/mac/Fenar/Zenxvio/product-works/payslip-generator`  
**Stack:** Next.js 15 App Router, TypeScript (strict), Prisma ORM, Tailwind CSS, Vitest, React Testing Library

---

## Active Workflows

Two parallel feature workflows are in progress. They are completely independent — never cross-modify files between them.

---

## Workflow 1 — Internal Messaging Platform

### Root branch
`feature/internal-messaging-platform`

### Phase overview
| Phase | Description | Status |
|-------|-------------|--------|
| Phase 1 — Static Design | Full static UI for all messaging surfaces | 🟡 In progress (final sprint in review) |
| Phase 2 — Domain Model and Foundation | Prisma schema, domain types, realtime contracts | ❌ Not started — blocked on Phase 1 gate |
| Phase 3 — Realtime | WebSocket transport, fanout, presence | ❌ Not started |

### Phase 1 — Static Design: Sprint Status

| Sprint | Branch | PR | Status | Tests |
|--------|--------|----|--------|-------|
| 1.1 — Workspace shell and navigation | `feature/internal-messaging-phase-1-sprint-1-*` | merged | ✅ Merged | 64 |
| 1.2 — Conversation list and reading workspace | `feature/internal-messaging-phase-1-sprint-2-*` | merged | ✅ Merged | 87 |
| 1.3 — Message composer and thread UX | `feature/internal-messaging-phase-1-sprint-3-*` | #331 | ✅ Merged | 27 |
| 1.4 — Channels, groups, and membership admin UX | `feature/internal-messaging-phase-1-sprint-4-channels-groups-admin` | #333 | 🟡 Pending review (remediated) | 38 |
| 1.5 — Tasks, meetings, and calendar UX | `feature/internal-messaging-phase-1-sprint-5-tasks-meetings-calendar` | #334 | 🟡 Pending review (remediated, stacked on 1.4) | 33 |
| 1.6 — Search, files, notifications, and final polish | `feature/internal-messaging-phase-1-sprint-6-search-files-notifications` | #335 | 🟡 In review (stacked on 1.5) | 31 |

**Total tests across all 6 sprint suites: 283 passing (as of Sprint 1.6 delivery)**

### Phase 1 Sprint File Map

All files live in `src/app/app/messaging/`:

```
messaging-workspace.tsx              — root layout, state orchestration
messaging-workspace-pane.tsx         — right-side context pane (files, admin)
messaging-reading-workspace.tsx      — conversation reading area (channel/DM/group)
messaging-conversation-list.tsx      — left rail conversation list
messaging-command-bar.tsx            — top search bar + notification bell
messaging-composer.tsx               — message compose bar
messaging-thread-panel.tsx           — thread/reply view
messaging-channel-detail.tsx         — channel info slide-in
messaging-channel-create.tsx         — create channel modal
messaging-group-detail.tsx           — group info slide-in
messaging-group-create.tsx           — create group modal
messaging-admin-panel.tsx            — admin/governance panel
messaging-task-panel.tsx             — task management panel
messaging-task-create.tsx            — create task modal
messaging-meeting-panel.tsx          — meetings panel with calendar grid
messaging-meeting-schedule.tsx       — schedule meeting modal
messaging-search-panel.tsx           — inline search results panel (Sprint 1.6)
messaging-files-panel.tsx            — file browser panel (Sprint 1.6)
messaging-notifications-panel.tsx    — notifications fly-out panel (Sprint 1.6)
messaging-notification-preferences.tsx — notification settings modal (Sprint 1.6)
messaging-ui-primitives.tsx          — RadioPill, ToggleSwitch shared components
types.ts                             — all TypeScript types for the messaging module
mock-data.ts                         — all static mock data
__tests__/messaging-sprint-1-1.test.tsx
__tests__/messaging-sprint-1-2.test.tsx
__tests__/messaging-sprint-1-3.test.tsx
__tests__/messaging-sprint-1-4.test.tsx
__tests__/messaging-sprint-1-5.test.tsx
__tests__/messaging-sprint-1-6.test.tsx
```

### Phase 1 Design System (enforced in all components)

- `"use client"` at top of every component file
- `RadioPill` and `ToggleSwitch` imported from `./messaging-ui-primitives` — never copied
- All modals: `role="dialog"`, `aria-modal="true"`, Escape key dismiss via `useEffect + document.addEventListener("keydown")`
- `data-testid` on every independently testable element
- `focus-visible:ring-2 focus-visible:ring-[#DC2626]` on all interactive elements
- `aria-label` on all icon-only buttons
- Colors: `#1C1B1F` primary, `#79747E` secondary, `#E0E0E0` border, `#DC2626` red action
- `cn()` for class merging, `lucide-react` for icons

### Phase 1 Completion Gate

Phase 1 is complete when all 6 sprint PRs are merged into their stacked bases and ultimately into the phase root. Phase 2 (domain model, Prisma schema, realtime contracts) cannot begin until Phase 1 is fully merged.

### Sprint 1.4 Remediation (applied, PR #333 updated)
Commit: `64e3048c`
Fixes applied:
1. `RadioPill` buttons missing `role="radio"` and `aria-checked` — added globally
2. Hardcoded `MOCK_ADMIN_ENTRIES` moved out of component render — `useMemo` applied
3. Escape key cleanup: `removeEventListener` cleanup confirmed in all modals
4. Tab container missing `role="tablist"` — added to tab navigation

### Sprint 1.5 Remediation (applied, PR #334 updated)
Fixes applied:
1. `CalendarGrid` now accepts `now?: Date` prop — non-deterministic `new Date()` made injectable
2. Task detail fallback: if task ID not in `MOCK_TASK_DETAILS`, constructs minimal detail from `MOCK_TASKS` entry rather than returning null (silent failure fixed)
3. `aria-current="date"` added to today's cell in calendar grid

### Sprint 1.6 Review Findings (PR #335 — remediation prompt given, not yet applied)

Issues found:
1. 🔴 HIGH — `unreadCount={2}` hardcoded in `messaging-workspace.tsx` — disconnected from notification state. Badge never clears after "Mark all read". Fix: lift notification state to workspace level, derive `unreadCount` from real state.
2. 🟡 MEDIUM — `notifOpen` and `searchOpen` have no mutual exclusion — both panels can open simultaneously.
3. 🟢 LOW — `degraded` banner copy-pasted 3 times in `messaging-reading-workspace.tsx` — extract to `DegradedBanner` component.
4. 🟢 LOW — `RadioPill` wrapper div lacks `role="radiogroup"` — add with `aria-label={name}`.

**Remediation prompt has been given to user for Kimi K2.6. Expected total after fix: 284+ tests.**

---

## Workflow 2 — Mailbox Platform (Gmail Integration)

### Root branch
`feature/mailbox-platform`

### Phase overview
| Phase | Description | Status |
|-------|-------------|--------|
| Phase 1 — Static Design | Full static mailbox UI | ✅ Complete (all 6 sprints merged) |
| Phase 2 — Domain Model and Gmail Connection Backbone | Schema, OAuth, registry, permissions | 🟡 In progress (Sprint 2.4 prompt given, 2.3 in review) |
| Phase 3 — Sync, Ingestion, and Mailbox State | Gmail data ingestion, thread sync | ❌ Not started — blocked on Phase 2 gate |
| Phase 4 — Real Inbox Workspace and Core Thread Operations | Live inbox UI | ❌ Not started |
| Phase 5 — Compose, Send, Drafts, and Attachments | Compose flow | ❌ Not started |

### Phase 2 Sprint Status

| Sprint | Branch | PR | Status | Tests |
|--------|--------|----|--------|-------|
| 2.1 — Schema and provider contracts | `feature/mailbox-platform-phase-2-sprint-2-1-*` | #327 | ✅ Merged | 63 |
| 2.2 — Gmail OAuth and token lifecycle | `feature/mailbox-platform-phase-2-sprint-2-2-gmail-oauth-token-lifecycle` | #330 | ✅ Merged (remediated) | 56 |
| 2.3 — Mailbox registry and connection admin | `feature/mailbox-platform-phase-2-sprint-2-3-registry-connection-admin` | #332 | 🟡 In review (double-remediated) | 35 |
| 2.4 — Connection permissions and org-scoped visibility | `feature/mailbox-platform-phase-2-sprint-2-4-connection-permissions-visibility` | — | ❌ Sprint prompt given, not yet started | — |

**Total tests across Sprints 2.1–2.3: 154 passing**

### Phase 2 File Map

```
src/lib/mailbox/
  domain-types.ts          — MailboxConnectionRecord, MailboxConnectionStatus,
                             connectionRequiresReconnect(), connectionIsDegraded(),
                             connectionIsOperational(), cursorIsExpired()
  health.ts                — deriveMailboxHealth(), MailboxConnectionHealth,
                             EXPIRING_SOON_THRESHOLD_MS (24h)
  admin-shapes.ts          — MailboxConnectionListItem, toMailboxConnectionListItem()
                             (status typed as MailboxConnectionStatus, not string)
  read-shapes.ts           — MailboxConnectionSummary, MailboxRestrictedSummary,
                             MailboxAdminConnectionSummary (@deprecated),
                             MailboxHealthSummary (@deprecated)
  connection-service.ts    — listMailboxConnections, getMailboxConnection,
                             createMailboxConnection, updateMailboxConnectionStatus,
                             disableMailboxConnection
  credential-store.ts      — AES-256-GCM encrypted token storage
  oauth-service.ts         — Gmail connect/callback/refresh/disconnect
  audit.ts                 — logMailboxAudit(), logMailboxAuditTx()
  index.ts                 — public barrel exports
  __tests__/
    mailbox-sprint-2-1.test.ts   (63 tests)
    mailbox-sprint-2-2.test.ts   (56 tests)
    mailbox-sprint-2-3.test.ts   (35 tests)

src/app/api/mailbox/
  connections/route.ts                        — GET list (admin-only)
  connections/[connectionId]/route.ts         — GET single + DELETE disable (admin-only)
  connections/[connectionId]/status/route.ts  — PATCH status update (admin-only)
  gmail/connect/route.ts                      — GET initiate OAuth
  gmail/callback/route.ts                     — GET OAuth callback
  gmail/refresh/[connectionId]/route.ts       — POST token refresh
  gmail/disconnect/[connectionId]/route.ts    — DELETE revoke and disconnect

src/app/api/integrations/
  _auth.ts                 — requireIntegrationAdminRoute()
```

### Phase 2 Codebase Standards (enforced everywhere)

- `import "server-only"` at top of every server module
- No raw Prisma in route handlers — all DB access through service layer
- Every route: `requireIntegrationAdminRoute()`, `rateLimitByOrg()`, full try/catch
- `Promise<NextResponse>` explicit return type on all route handlers
- No `any` types
- Audit events via `logMailboxAuditTx()` for all governance mutations
- Org-scoping on every DB query
- AES-256-GCM encryption for all OAuth credentials at rest

### Sprint 2.3 Remediation History (fully applied, PR #332 updated)

**First review — commit `0945f52d`:**
1. `@deprecated` annotations added to `MailboxHealthSummary` and `MailboxAdminConnectionSummary` in `read-shapes.ts`
2. TOCTOU double-read removed from PATCH status route
3. `status` field in `MailboxConnectionListItem` retyped from `string` to `MailboxConnectionStatus`
4. `DELETE /api/mailbox/connections/[connectionId]` route added

**Second review — commit `c354bff3`:**
Results: no further critical issues found. Branch is ready for approval.

### Sprint 2.4 — Connection Permissions and Org-Scoped Visibility (NEXT)

**Stacked on:** `feature/mailbox-platform-phase-2-sprint-2-3-registry-connection-admin`  
**Sprint prompt given to:** Kimi K2.6  
**Status:** Not yet started

What Sprint 2.4 will build:
- `MailboxVisibilityPolicy = "org_shared" | "restricted" | "admin_only"`
- `MailboxAccessLevel = "full" | "read_only" | "none"`
- `MailboxAccessResolution` — per-user, per-connection access evaluation result
- `resolveMailboxAccessLevel()` — pure function with deterministic rule evaluation
- `canAccessMailbox()` — predicate helper
- `src/lib/mailbox/visibility-service.ts` — `getMailboxAccessResolution`, `listMailboxConnectionsForMember`, `setMailboxVisibilityPolicy`
- Prisma schema: add `visibilityPolicy String @default("org_shared")` to `MailboxConnection`
- `requireIntegrationMemberRoute()` in `_auth.ts` — non-admin auth guard for visibility routes
- `GET /api/mailbox/connections/visible` — member-accessible, returns `{ accessible, restricted }`
- `PATCH /api/mailbox/connections/[id]/policy` — admin governance, emits audit event
- `CONNECTION_POLICY_UPDATED` audit action
- 29 new tests → expected total: **183+ passing** across all 4 sprint suites

### Phase 2 Completion Gate

Phase 2 is complete when Sprint 2.4 is merged. Gate criteria:
- Mailbox schema and provider contracts stable
- Gmail connections can be securely created, refreshed, and governed
- Mailbox visibility and access rules locked (Sprint 2.4)
- Phase 3 (sync/ingestion) can rely on stable mailbox identifiers and auth lifecycle

---

## PR Merge Order (Strict — Never Merge Out of Order)

### Mailbox Platform
```
2.1 (✅ merged) → 2.2 (✅ merged) → 2.3 (🟡 approve #332 next) → 2.4 (❌ sprint not started)
```

### Internal Messaging Platform
```
1.1 (✅) → 1.2 (✅) → 1.3 (✅) → 1.4 (🟡 approve #333 next) → 1.5 (#334, stacked) → 1.6 (#335, stacked)
```

---

## Workflow Isolation Rule

These two workflows NEVER share files. Any AI working on one workflow must not touch files from the other:

| Workflow | Scope |
|----------|-------|
| Internal Messaging | `src/app/app/messaging/**` |
| Mailbox Platform | `src/lib/mailbox/**`, `src/app/api/mailbox/**`, `src/app/api/gmail/**` |

---

## AI Handoff Pattern

All sprint prompts follow this pattern when given to external AI (Kiro, Kimi K2.6, etc.):
1. Full context block (project, stack, completed history)
2. Read-first instruction (`cat` all relevant files before writing)
3. Branch creation command
4. Stacked PR note with explicit "do NOT merge until parent PR is approved"
5. Numbered task list with exact file names, types, function signatures
6. Test requirements (explicit test names and expected count)
7. Validation commands (`prisma validate`, `tsc --noEmit`, `npm run test`)
8. Commit and push commands
9. Report-back requirements (test counts, PR URL, TypeScript clean)

Review prompts follow this pattern:
1. Scope warning (do not touch other workflows)
2. Read-first instruction
3. Review categories (security, correctness, type safety, design consistency, test coverage)
4. Issue classification (🔴 HIGH / 🟡 MEDIUM / 🟢 LOW)
5. Fix instructions (targeted, minimal changes)
6. Validation and commit

---

## Test Infrastructure

- **Framework:** Vitest with React Testing Library
- **Pattern:** `vi.mock("server-only", () => ({}))` at top of every server test
- **DB mocking:** `vi.mock("@/lib/db", () => ({ db: { ... } }))` — never real DB in tests
- **Auth mocking:** `vi.mock("@/lib/auth", () => ({ getOrgContext: vi.fn(), hasRole: vi.fn() }))`
- **Rate limit mocking:** `vi.mock("@/lib/rate-limit", () => ({ rateLimitByOrg: vi.fn().mockResolvedValue({ success: true, remaining: 59 }), RATE_LIMITS: { api: { maxRequests: 60, window: "60 s" } } }))`
- **Transaction mocking:** `mockDb.$transaction.mockImplementation(async (cb) => cb(mockDb))`

---

## Key Dates

| Event | Date |
|-------|------|
| Sprint 1.3 merged (PR #331) | ~2026-05-09 |
| Sprint 2.2 merged (PR #330, remediated) | ~2026-05-09 |
| Sprint 1.4 reviewed and remediated | 2026-05-09 |
| Sprint 1.5 reviewed and remediated | 2026-05-10 |
| Sprint 2.3 double-remediated (c354bff3) | 2026-05-10 |
| Sprint 1.6 delivered (PR #335) | 2026-05-11 |
| Sprint 1.6 reviewed — remediation prompt given | 2026-05-11 |
| Sprint 2.4 prompt given | 2026-05-11 |
| This context snapshot | 2026-05-11 02:47 IST |
