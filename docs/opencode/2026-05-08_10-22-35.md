# Tagging Initiative — Session Context

**Date:** 2026-05-08 10:22 AM
**Branch:** `feature/platform-rebrand-redesign-tagging`
**Last Commit:** `2a13392b` fix(tagging): pass tag props to InvoicePanel component

---

## Current State

All 5 tagging phases merged into `feature/platform-rebrand-redesign-tagging`:

| Phase | Content |
|-------|---------|
| 1 | Tag schema, catalog CRUD, assignment service |
| 2 | Document workflows, tag picker, bulk, list filters |
| 3 | Reports, CSV exports, analytics hub, drill-down |
| 4 | Customer/vendor defaults, suggestions, management console |
| 5 | Permissions, audit logging, API routes, telemetry, docs |

## Final Remediation (PR #313)

- PATCH /api/tags/[id] supports archive/unarchive without name
- hasRole checks on all 6 customer/vendor default-tag mutations
- Telemetry wired into invoice/voucher workspaces and analytics page
- 46 tests pass with zero unhandled errors
- Release docs updated

## Active Worktrees

- Main: `/Users/mac/Fenar/Zenxvio/product-works/payslip-generator`
- Tagging: `/private/tmp/payslip-tagging-prd` (detached HEAD)

## Key Files

- Tag picker: `src/features/tags/components/tag-picker.tsx`
- Settings registry: `src/components/settings/settings-registry.tsx`
- Auth blob: `src/features/auth/components/auth-blob-background.tsx` (line 144 of layout)
- `src/lib/money.ts` — has local modifications (unrelated, do not touch)

## Running

```bash
cd /Users/mac/Fenar/Zenxvio/product-works/payslip-generator
rm -rf .next
npm run dev
```

## Tips

- Create tags first at `/app/settings/tags` or they won't appear in picker
- Kill port conflicts: `lsof -ti:3001 | xargs kill -9`
- Wave animation on `/auth/login` is working
