# Project Context

## Current Branch
`feature/platform-rebrand-redesign`

## Recent Changes (Last Session)

### Auth Page Theme Split
- **Signup page (`/auth/signup`)**: Purple theme (`#C05092`)
  - Wave background: purple
  - "Create account" button: purple
  - "Sign in" link: purple

- **Sign in page (`/auth/login`)**: Red theme (`#DC2626`)
  - Wave background: red
  - "Sign in" button: red
  - "Forgot password?" link: red
  - "Sign up" link: red
  - Checkbox: red accent

- **All other auth pages**: Red theme (forgot-password, verify-email, OTP, 2FA, accept-invite)

### Implementation Details
- `AuthBlobBackground` component now accepts a `variant` prop: `"red"` | `"purple"`
- `auth/layout.tsx` uses `usePathname()` to detect `/auth/signup` and passes `variant="purple"`
- All other routes get `variant="red"`
- Canvas draws with appropriate color palette based on variant

## Key Files Modified
- `src/features/auth/components/auth-blob-background.tsx` — Added variant prop with color palettes
- `src/app/auth/layout.tsx` — Path-based variant selection
- `src/app/auth/signup/page.tsx` — Purple link color
- `src/app/auth/login/login-form.tsx` — Red theme colors
- `src/app/auth/forgot-password/page.tsx` — Red theme colors
- `src/app/auth/verify-email/page.tsx` — Red theme colors
- `src/app/auth/otp/page.tsx` — Red theme colors
- `src/app/auth/accept-invite/page.tsx` — Red theme colors
- `src/app/auth/2fa/2fa-form.tsx` — Red theme colors
- `src/components/ui/button.tsx` — Reverted to red hover
- `src/app/globals.css` — `--brand-cta` back to red

## Previous Context
- `feature/platform-rebrand-redesign-tagging-studio-redesign` branch merged via PR #314
- `DocumentWorkspaceLayout` rebuilt with left-right split, no sticky header
- `DocumentPreviewSurface`: zoom controls, dynamic content height
- Tag picker moved to side panel, uses `/api/tags` REST endpoint
- Text color fixes applied across all templates (invoice, voucher, salary-slip)
- Landing page liquid blob animation merged via PR #317

## Branch Status
- `feature/platform-rebrand-redesign` — main feature branch (current)
- `feat/landing-wave-animation` — merged into feature branch

## Next Steps
- Test signup page purple theme
- Test login page red theme
- Verify wave animation colors on both pages
