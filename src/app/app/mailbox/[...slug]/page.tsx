import { MailboxWorkspace } from "../mailbox-workspace";

export const metadata = {
  title: "Mailbox — Slipwise",
};

export default function MailboxCatchAllPage() {
  return <MailboxWorkspace />;
}
